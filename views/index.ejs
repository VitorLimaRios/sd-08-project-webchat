<!DOCTYPE html>
<html>
  <head>
    <title>WebChat</title>
  </head>
  <body>
    <script src="http://localhost:3000/socket.io/socket.io.js"></script>
    <div>
      <ul id="messagem"></ul>
    </div>
    <form action="">
      <input id="messagemInput" autocomplete="off" /><Button>Send</Button>
    </form>
    <script>
      const socket = io();
      const form = document.querySelector('form');
      const inputMessage = documnet.querySelector('#messagemInput');

      form.addEventListener('submit', (e) => {
        e.preventDefault();
        socket.emit('messagem', inputMessage.value);
        inputMessage.value = '';
        return false;
      })

      const createMessage = (message) => {
        const messagesUl = document.getElementById('messagem');
        const li = document.createElement('li');
        li.innerText = message;
        messageUl.appendChild(li);
      }

      socket.on('message', (message) => createMessage(message));
      socket.on('newConnection', (obj) => createMessage(obj.message));
    </script>
  </body>
</html>